[project]
name = "integritee"
version = "0.1.0"
description = "Public attestation ledger for Cohere's confidential computing models"
requires-python = ">=3.10"
license = "Apache-2.0"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "jsonschema>=4.20",
    "pyyaml>=6.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
